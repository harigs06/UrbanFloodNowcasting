global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'urban_flood_engine'
    static_configs:
      - targets: ['api:8000', 'host.docker.internal:8000', 'localhost:8000']
    metrics_path: '/metrics'
